import { makeStyles } from '@mui/styles';

// Styles
export const useStyles = makeStyles(() => ({
  toggleText: {
    fontSize: '14px',
    color: '#323A45',
    lineHeight: '16px',
    fontWeight: '400',
  },
}));
